<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/salmkidsicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/salmkidsicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.SALMKids"
        tools:targetApi="31">
        <activity
            android:name=".LicaoAudioPalavra"
            android:exported="false" />
        <activity
            android:name=".TelaConfig"
            android:exported="false" />
        <activity
            android:name=".TelaLinguagens"
            android:exported="false" />
        <activity
            android:name=".TelaInicial"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts"/>
    </application>

</manifest>